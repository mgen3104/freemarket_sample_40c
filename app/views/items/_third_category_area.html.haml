= form_with model: @item, :html => {:class => "sell-form"} do |f|
  = f.select :third_category_id, options_from_collection_for_select(@third_categories, :id, :name), {prompt: '---'}, {data: {remote: true, url: url_for(action: :select_third_category)}}
  %i.fas.fa-angle-down
